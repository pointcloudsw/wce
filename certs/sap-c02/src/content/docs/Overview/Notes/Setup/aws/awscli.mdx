---
title: AWS CLI install and setup
description: AWS CLI install and setup
---

```sh
cd ~
sudo apt update
sudo apt install unzip
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install
```